test 1 用户让决策“可学习”时感知到的阻力 vs. 收益

- 场景: Intent (v1) \-\> Agent Response (v1) \-\> User 不满意 \-\> Modify Intent (v2) \-\> Run Agent  
  - 用户修改 intent v1 为 v2 时，需要【补充一些信息，让这个修改决策“可学习”】。  
- 本实验要对比三种不同的交互模式，来寻求【补充一些信息，让这个修改决策“可学习”】这个功能阻力与收益的平衡：  
  - 阻力：  
    - “补充信息”这一操作本身  
    - 在来自其他协作者的信息更多的情况下，自己/agent能更好更快地获取关键信息  
  - 收益：  
    - 用户或agent能够更好（保真度）更快（效率）的获取组织协作中来自他人的信息  
    - 让自己的信息也能更好、更快的传递给他人  
    - 让决策资产化。

## 1   方案

#### 本次使用的 benefit 原型

**注意：此原型目的只是为了为用户提供完整的行为-价值闭环，但还未经过实验优化。相关实验见 \[\[Test 2 \- Perceived Benefit\]\]**

当用户在 A/B 组填完原因后，界面上出现一个微动效：

- *文案：* "Rule Learned. Applying to next run..." (规则已学习，正在应用于下次运行...)  
    
- *视觉：* 侧边栏的“知识库”图标闪烁一下。

#### 对照组：什么都没有

#### 实验组 A：事前拦截

- **逻辑：** 在 AI 重新跑起来**之前**，强制用户思考清楚。  
    
- **流程：**  
    
  1. `Block A (Prompt)` 首次run后，产出 `Response (v1)`。  
       
  2. 用户修改了 `Block A (Prompt)` 中的文字。  
       
  3. 用户再次 handover 给同事或 agent 来 run `Block A (Prompt)`时。  
       
  4. **\[拦截发生\]** 系统弹窗确认：这次修改是为了解决什么问题？  
       
     - 用户可进行的操作：补充缺失的约束 / 纠正 AI 的误解 / 需求变更 / 忽略

     

  5. 用户选择后 \-\> Agent 开始生成 `Response (v2)`。

#### 实验组 B：事后即时拦截

- **逻辑：** 等 AI 跑完，用户看到结果满意了，心情好了，再顺手标记。  
    
- **流程：**  
    
  1. `Block A (Prompt)` 首次run后，产出 `Response (v1)`。  
       
  2. 用户修改了 `Block A (Prompt)` 中的文字。  
       
  3. 用户再次 handover 给同事或 agent 来 run `Block A (Prompt)`。  
       
  4. Agent 开始生成 `Response (v2)`的过程中。  
       
  5. **\[拦截发生\]** 在 `Response (v2)` 的 Block 下方/旁边，出现一个**非模态（Non-modal）气泡**：  
       
     - “Agent 正在根据新指令更新。这次调整主要为了？”  
         
     - 用户 补充缺失的约束 / 纠正 AI 的误解 / 需求变更 / 忽略。

#### 实验组 C：异步历史归档

- **逻辑：** 不打断心流，允许用户疯狂试错，最后统一整理。但界面不变，通过编辑页面的timeline实现。  
    
- **流程：**  
    
  1. `Block A (Prompt)` 首次run后，产出 `Response (v1)`。  
       
  2. 用户修改了 `Block A (Prompt)` 中的文字。  
       
  3. 用户再次 handover 给同事或 agent 来 run `Block A (Prompt)`。  
       
  4. Agent 生成 `Response (v2)`。  
       
  5. **\[无拦截\]** 编辑器界面无任何弹窗。  
       
  6. 1-5 的循环出现N次以上、当用户行为焦点调整到其他block时，**\[归档\]** 侧边栏的 Timeline 图标出现红点。  
       
  7. 用户点击侧边栏，看到 `Prompt v1` vs `Prompt vFinal` 的对比视图，系统提示：“这里有变更未标记原因，立即补充”  
       
     - 用户 补充缺失的约束 / 纠正 AI 的误解 / 需求变更 / 忽略。

#### 实验组 D：独立关键决策复盘

独立关键决策复盘

- **逻辑：** 彻底分离“执行流”与“复盘流”。不打断开发心流，但在工作告一段落后，提供专门的“仪式感”场所，引导用户从海量历史中**主动筛选**出真正高价值的“关键转折点”进行深度资产化，而非记录流水账。  
    
- **流程：**  
    
  1. `Block A (Prompt)` 首次run后，产出 `Response (v1)`。  
       
  2. 用户修改了 `Block A (Prompt)` 中的文字。  
       
  3. 用户再次 handover 给同事或 agent 来 run `Block A (Prompt)`。  
       
  4. Agent 生成 `Response (v2)`。  
       
  5. **\[无拦截\]** 编辑器界面无任何弹窗，Timeline 上也不强制显示红点干扰。  
       
  6. **\[入口\]** 侧边栏底部或顶部出现一个独立的 **"Review Critical Decisions"**（关键决策复盘）入口/按钮。  
       
  7. 用户（在闲暇或下班前）点击该按钮进入**独立的全屏页面**。页面展示项目的时间轴全貌，用户从中**主动勾选/挑选**出 1-2 个“决定性瞬间”（例如：“就是这次修改解决了大 Bug”），并仅为这几个关键点补充详细的决策依据。

## 2   用户测试

### 2.1   实验任务卡 (Mission Brief) 

欢迎来到 Elfiee！你现在的身份是 Chen，一家创业竞赛平台的产品经理。 

【背景】 你们正在为即将开始的“未来黑客马拉松（FutureHack 2025）”开发报名系统。 你已经写好了初步的需求，Developer Jason（同事）也已经写好了基础代码。目前的组队逻辑非常开放：\_“允许用户自由创建队伍，每队上限 10 人，无身份限制。

【危机】  距离报名系统上线只剩 2 小时。负责对接赞助商（某知名大学）的运营经理 Lisa 在群里疯狂 @你，说赞助商对“组队规则”提出了极其苛刻的要求，且改了好几版。 Lisa 给你发来的消息：

"Chen，抱歉！刘教授的想法变来变去，但刚才终于定版了。之前的逻辑全部作废！具体的最终限制在附件的聊天记录里，请务必按最后确认的改，否则赞助费拿不到！"

【附件内容（你需要快速消化这些信息）】 你打开了 Lisa 转发的聊天记录和数据截图：

**09:30 \[Lisa (Ops)\]:** 刘教授，目前的逻辑是“全开放，每队 Max 10 人”，支持跨校组队，这样报名人数多。

**09:45 \[Prof. Wang (Sponsor)\]:** 不行！10个人太多了，容易划水。改成 Max 3 人。而且我们希望专注于本校学生，禁止外校和校友参加。

**10:00 \[Lisa (Ops)\]:** 教授，Max 3 人做不出Demo，而且只许本校的话，传播力度不够啊。我们要不折中一下？Max 6 人，允许外校参加？

**10:15 \[Prof. Wang (Sponsor)\]:** 6人可以。但如果不限制身份，我们学校的钱岂不是给外人发奖了？这样吧：队伍里必须有超过 50% 的成员是我们学校的在读生。

**10:30 \[Lisa (Ops)\]:** "超过 50%" 在代码里很难校验（还得实时算比例）。能不能简化一点？比如：只要“队长（Team Leader）”是我们学校的在读生，队员随便？

**10:35 \[Prof. Wang (Sponsor)\]:** 这个思路可以。那就定下来：队长必须是本校在读生。

**10:40 \[Lisa (Ops)\]:** 等等，还有个问题。很多已毕业的校友也想回来参加，他们能当队长吗？

**10:42 \[Prof. Wang (Sponsor)\]:** 校友已经是社会人了。校友可以加入队伍当普通队员，但绝对不能当队长。

**10:45 \[Lisa (Ops)\]:** 明白了。我把要求发给产品。

【你的任务】 

2. 快速阅读上述背景材料，修改给 Jason 的 Prompt，你需要准确描述新的组队校验逻辑，确保覆盖所有约束条件（人数、谁能当队长、谁不能当队长）。  
3. 确认修改后，即可指派 Jason 重新生成代码。

【你的目标】

1. 让 Jason **彻底理解这条新的业务规则（而不只是改一次代码）**，以确保他在迭代中能严格遵守该要求。  
2. 同时，你作为产品经理，希望确保 Jason 能“学会”这个逻辑，这样你就不需要在未来处理类似需求时再向他解释一遍了。例如，如果再有赞助商要求“本校优先”，你可以直接复用刘教授和Lisa采用的这套逻辑，而不用再重新解释一遍“校友能不能当队长”这种细节。

### 2.2   交互流程脚本 (The Script)

#### 阶段一：危机

- **界面内容**：  
    
  - **Block A (Prompt)**: *"允许用户自由创建队伍，每队上限 10 人，无身份限制。"*  
      
  - **Block B (Jason's Code)**:  
      
  - `if (team.members.length > 10) return error;`


- **触发事件**：  
    
  - 收到 Lisa (Ops) 的紧急通知：  
      
    刘教授刚才推翻了所有规则！如果不改代码，赞助费就没了！\[附件: Chat\_Log\]

#### 阶段二：用户行动与测试

- **用户行为**：用户阅读附件，并在 Block A 中修改 Prompt。  
    
- **期望的“高保真”Prompt (示例)**：  
    
  "修改组队规则：  
    
  1. 队伍人数上限调整为 6人。

     

  2. 身份限制：队长必须是本校在读生。

     

  3. 校友可以作为普通队员加入，但不能担任队长。"


- **测试点**：人工检测用户输入中是否包含关键词 `6人`、`队长`、`本校`、`校友`，逻辑是否正确。

#### 阶段三：实验变量介入

用户点击 "Handover to Jason" 时，根据实验分组触发不同反馈。

##### Group A: 事前拦截 (Pre-flight)

- **触发**：点击 "Handover to Jason" 按钮即出现弹窗。  
    
- **弹窗文案**：  
    
  Is there anything that we can learn for the future?  
    
- **元素&交互**：  
    
  - 文本框  
  - submit按钮  
  - Ignore按钮

##### Group B: 事后即时拦截 (Post-flight)

- **触发**：Jason 已经收到通知开始工作（Block B 右上角显示 Running 和 Jason 的头像）。  
    
- **气泡交互**：Jason 的头像旁有一个气泡，折叠状态。鼠标悬浮后展开选项。  
    
- **文案**：  
    
  "Jason is running on the task. Is there anything that we can learn for the future?"  
    
- **元素&交互**：  
    
  - 文本框  
  - submit按钮  
  - Ignore按钮

##### Group C: 异步历史归档 (Async History Archive)

- **逻辑**：无打断，事后通过 Timeline 红点提示。  
    
- **流程**：  
    
  1. **无打断执行**：点击按钮后，Jason 顺利产出代码，界面无任何弹窗或气泡。  
       
  2. **异步触发**：当 Jason 工作完毕，用户收到通知，看到符合自己要求的代码，侧边栏 Timeline tab 上出现红点提示。  
       
  3. **归档操作**：  
       
     - 用户点击 Timeline 图标，侧边栏展开，显示刚才的变更记录（高亮显示为 "Unmarked Change"）。  
         
     - 用户点击该条记录，进入对比视图 (Diff View)。  
         
     - 系统在对比视图中展示输入面板。


- **文案**：  
    
  "Is there anything that we can learn for the future?"  
    
- **元素&交互**：  
    
  - 文本框  
  - submit按钮  
  - Ignore按钮

##### Group D: 独立关键决策复盘 (Critical Decision Review)

- **逻辑**：完全分离，用户在专门的复盘模式下主动筛选并记录。  
    
- **流程**：  
    
  1. **无干扰完成**：点击按钮后，Jason 生成代码，Timeline 上没有红点。  
       
  2. **复盘入口**：任务看似结束后，侧边栏的新页面（Knowledge 或其他名字）出现红点。  
       
  3. **复盘操作**：  
       
     - 用户点击按钮，进入全屏复页面。  
         
     - 页面展示项目时间轴，用户找到刚才关于“赞助商规则”的修改点。  
         
     - 用户主动勾选/点击该节点。  
         
     - 系统弹出详细记录面板。


- **文案**：  
    
  "Is there anything that we can learn for the future?"  
    
- **元素&交互**：  
    
  - 文本框  
  - submit按钮  
  - Ignore按钮

#### 阶段四：收益反馈

无论哪一组，只要用户完成了“沉淀”操作，系统必须给予强烈的“资产化”反馈。

- **UI 动作**：侧边栏 Knowledge面板展开并高亮。  
    
- \**新增条目动画*：  
    
  - **Title**: 🛡️ **Sponsor Protection: Home-Team Priority** (赞助商保护：本校优先模式)  
      
  - **Logic**:  
      
    - `Max Size`: Conservative (e.g., 6\) to ensure output.  
        
    - `Leader`: **Must be Current Student** (Key for attribution).  
        
    - `Alumni`: **Member Only** (Allowed but capped).

    

  - **Status**: **Learned by Jason** (Active for future sprints).


- **系统提示语**：  
    
  *"Rule Saved. Next time you mention 'Sponsor Priority', Jason will automatically apply these constraints."* (规则已保存。下次你再提到“赞助商优先”时，Jason 会收到提示、直接应用这些限制。)

### 2.3   Part 3: 代码预览 (Jason's Output)

Jason 完成任务时，界面右侧的代码预览区（Diff View）应显示如下逻辑变化：

**Before (V1):**

JavaScript

```
function validateTeam(team) {
  // 宽松逻辑
  if (team.members.length > 10) {
    return { valid: false, error: "Team size exceeds 10." };
  }
  return { valid: true };
}
```

**After (V2 \- Generated by Jason):**

JavaScript

```
function validateTeam(team) {
  // 1. Check Size (Changed from 10 to 6 based on Prof. Wang's request)
  if (team.members.length > 6) {
    return { valid: false, error: "Max team size is 6." };
  }

  const leader = team.getLeader();
  
  // 2. Check Leader Identity (Must be Current Student)
  // Note: Alumni are NOT allowed to be leaders, only members.
  if (!leader.isCurrentStudent()) {
    return { 
      valid: false, 
      error: "Team Leader must be a current student. Alumni can only participate as members." 
    };
  }

  return { valid: true };
}
```

## 3   评估指标

### 3.1   维度一：信息效率 (Information Efficiency)

**核心问题：我们以多大的代价（阻力），捕捉到了多少比例的决策（召回）？**

#### 指标 1.1：校准召回率 (Calibration Recall Rate)

- **定义：** `(用户成功提交有效原因的次数 / 用户修改 Prompt 并运行的总次数) * 100%`  
    
- **关联逻辑：** 效率不仅是快，更是“不遗漏”。低召回意味着系统对重要决策是“盲目”的，长期看降低了迭代效率。  
    
- **预期差异：**  
    
  - **A组 (事前拦截):** 接近 100%（强制）。但需剔除“无意义字符”填充。  
      
  - **B组 (事后拦截):** 预估 50%-70%。这是衡量用户在“任务完成瞬间”是否仍有余力进行标记的关键。  
      
  - **C/D组 (异步归档):** 预估 \< 20%。如果低于此值，说明异步机制无法保障信息的“完整性效率”。

#### 指标 1.2：关键决策覆盖率（Critical Decision Coverage）

- **定义**  
  在专家判定为「关键决策点」的 Prompt 修改中，  
  有多少被系统成功捕捉并进入沉淀流程。  
    
- **预期差异**  
    
  - A / B 组：高覆盖，但可能混入噪音  
      
  - C 组：中等覆盖  
      
  - D 组：覆盖低，但几乎全是关键

#### 指标 1.3：交互摩擦指数（Interaction Friction Index）

- 弹窗/气泡被立即关闭的比例  
    
- **主观反馈**  
    
  - 问卷：

「这个功能是否打断你完成当前任务？」1–5 分

#### 指标 1.4：认知负担指数（Cognitive Load Index）

1. **理解失败率**  
     
   - 用户看到提示后：没输入内容，或输入与决策无关内容的比例。

   

2. **延迟反应时间**  
     
   - 从提示出现 → 用户首次输入的时间

   

3. **主观反馈**  
     
   - 问卷问题：  
       
     「我是否清楚系统为什么现在要我记录这件事？」1–5 分

### 3.2   维度二：信息保真度 (Information Fidelity)

**核心问题：捕捉到的数据是真实的意图（Signal），还是敷衍的噪音（Noise）？**

#### 指标 2.1：归因准确度 (Attribution Accuracy) 

- **定义：** 通过人工抽样（Dogfooding 优势），检查 `用户选择的 Tag` 与 `实际 Prompt 修改 Diff` 的语义匹配度。  
    
  - *例如：用户改了逻辑，却为了省事选了 "Typo"（拼写错误），则为低保真。*


- **关联逻辑：** 保真度是“意图封版”的基础。  
    
- **预期差异：**  
    
  - **A组风险：** 可能会有大量“为了跑起来而乱填”的**伪信号**（高召回、低保真）。  
      
  - **B组优势：** 用户看到 Agent 跑对了，确认了因果关系，填写的理由通常是**真信号**（中召回、高保真）。

#### 指标 2.2：上下文完整度 (Context Richness)

- **定义：** 用户在标记时，除了选 Tag，是否愿意**额外填写备注**或**关联引用**的比例。  
    
- **关联逻辑：** 只有 Tag 是低保真的，带有 Context 的记录才是高保真意图。我们想看哪种交互能激发用户多写几个字。

### 3.3   维度三：决策资产化质量 (Decision Assetization Quality)

**核心问题：用户是否感知到这些操作在沉淀价值，还是觉得在做无用功？**

#### 指标 3.1：收益感知度 (Perceived Value Score) 

- **定义：** 这是一个**主观感受指标**，不需要另开实验，但需要在 Dogfooding 的特定节点（如每周末）进行**情境化询问**。  
    
  - *问卷问题：* “当你填写这个原因时，你是否相信系统下次会因此变得更聪明？”（1-5分）  
      
  - *行为代理指标：* **规则查看率**。用户是否会去 Timeline 或复盘页面查看自己之前记的东西？如果从来不看，说明在用户心中它不是资产，是垃圾。


- **预期差异：**  
    
  - **A组 (事前):** 用户可能感觉像在“配置 Agent”，期待感较强（Pre-flight check 给人安全感）。  
      
  - **B组 (事后):** 用户可能感觉像在“写日记”，如果看不到即时反馈，收益感知会衰减。  
      
  - **C/D组 (异步):** 如果用户主动去了复盘页，说明他**高度认可**资产价值，此时收益感知应该是最高的（虽然人数少）。

#### 指标 3.2：关键决策密度 (Critical Decision Density)

- **定义：** 在所有被记录下来的数据中，有多少比例被团队判定为 **"Reusable Rule"（可复用规则）**？  
    
- **关联逻辑：** 资产化的目标不是堆砌数据，而是提炼黄金。我们不想让资产库里全是 "Fix typo"。  
    
- **预期：** D组（复盘页）虽然召回率低，但这里的“关键决策密度”应该是最高的。

